{
  "name": "codediary",
  "version": "1.0.3",
  "description": "",
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublish": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bin": "dist/index.js",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@codediary/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@codediary/local-api": "^1.0.3",
    "@types/node": "^17.0.12",
    "commander": "^8.3.0",
    "esbuild": "0.8.26",
    "typescript": "^4.5.5"
  }
}
